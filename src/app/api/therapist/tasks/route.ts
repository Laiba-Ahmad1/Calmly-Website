// POST /api/therapist/tasks — therapist assigns a task to a patient
// PATCH /api/therapist/tasks — patient marks a task complete
export async function POST(req: Request) {
  return new Response(null); // TODO
}
export async function PATCH(req: Request) {
  return new Response(null); // TODO
}
