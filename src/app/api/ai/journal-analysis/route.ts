// POST /api/ai/journal-analysis — send one journal entry to AI, get back
// { mood, trigger, sleep, theme } structured extraction. No diagnosis language.
export async function POST(req: Request) {
  return new Response(null); // TODO
}
