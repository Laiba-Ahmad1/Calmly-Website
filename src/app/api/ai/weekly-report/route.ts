// POST /api/ai/weekly-report — aggregate last 7 days of patient data, call AI,
// return { summary, patterns, progress, attention_points, therapist_questions }
export async function POST(req: Request) {
  return new Response(null); // TODO
}
