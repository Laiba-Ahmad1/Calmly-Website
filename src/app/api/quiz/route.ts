// GET /api/quiz — quiz history for patient
// POST /api/quiz — submit an answer, update category weights
export async function GET(req: Request) {
  return new Response(null); // TODO
}
export async function POST(req: Request) {
  return new Response(null); // TODO
}
