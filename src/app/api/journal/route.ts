// GET /api/journal — list entries for logged-in patient
// POST /api/journal — create entry, run local sentiment scoring, optionally call AI feedback
export async function GET(req: Request) {
  return new Response(null); // TODO
}
export async function POST(req: Request) {
  return new Response(null); // TODO
}
