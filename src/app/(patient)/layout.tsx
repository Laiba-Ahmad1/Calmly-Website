// Patient shell — hosts the radial hub nav (not a sidebar), auth-guards patient routes
export default function PatientLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>; // TODO: <NavHub />
}
