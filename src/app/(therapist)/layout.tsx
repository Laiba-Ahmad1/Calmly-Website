// Therapist shell — auth-guards therapist routes, simple top-level nav
export default function TherapistLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>; // TODO
}
